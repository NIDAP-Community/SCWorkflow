% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Trajectory_CellTypes_v12.R
\name{Trajectory_CellTypes}
\alias{Trajectory_CellTypes}
\title{Trajectory analysis with TSCAN}
\usage{
Trajectory_CellTypes(
  Seurat_Object,
  MetaData,
  Clusters_to_Use,
  Custom_Gene_List = NULL
)
}
\arguments{
\item{Seurat_Object}{A Seurat object or path to an RDS file containing one.}

\item{MetaData}{A data.frame with per-cell metadata; must contain the clustering column.}

\item{Clusters_to_Use}{Character scalar naming the metadata column to use for clusters.}

\item{Custom_Gene_List}{Optional character vector or delimited string of genes to highlight.}
}
\value{
Invisibly returns \code{NULL}. Generates trajectory and pseudotime plots as side effects.
}
\description{
Perform the CCBR single-cell RNA-seq trajectory workflow using TSCAN.
}
\examples{
\donttest{
if (requireNamespace("Seurat", quietly = TRUE)) {
  seurat_example <- Seurat::CreateSeuratObject(
    counts = matrix(rpois(200, lambda = 5), nrow = 20,
                    dimnames = list(paste0("g", 1:20), paste0("c", 1:10)))
  )
  seurat_example <- Seurat::AddMetaData(
    object = seurat_example,
    metadata = data.frame(cluster = sample(letters[1:2], 10, replace = TRUE),
                          row.names = colnames(seurat_example))
  )
  Trajectory_CellTypes(
    Seurat_Object = seurat_example,
    MetaData = Seurat::FetchData(seurat_example, vars = c("cluster")),
    Clusters_to_Use = "cluster"
  )
}
}
}
